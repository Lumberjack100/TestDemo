<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />  <!-- 获取粗略位置权限 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- 获取精确位置权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- 如果设置了target >= 28 如果需要启动后台定位则必须声明这个权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /><!-- 读取外部存储权限 -->

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" /> <!-- 写入外部存储权限（注意：这个权限在 targetSdk >= Android 11 并且 Android 11 及以上的设备上面不起作用，请适配分区存储特性代替权限申请）-->

    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        android:minSdkVersion="30" /> <!-- 文件管理权限（特殊权限，Android 11 新增的权限）-->

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />  <!-- 读取图片权限（Android 13.0 新增的权限）-->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />   <!-- 读取视频权限（Android 13.0 新增的权限）-->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />   <!-- 读取音频权限（Android 13.0 新增的权限）-->

    <!-- 授予对照片和视频的部分访问权限（Android 14.0 新增的权限）-->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        android:minSdkVersion="33" /> <!-- 发送通知权限（Android 13.0 新增的权限）-->

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" /> <!-- 安装应用权限，更新自身及下载安装app（特殊权限，Android 8.0 新增的权限）-->

    <uses-permission android:name="android.permission.CAMERA" />  <!-- 相机权限 -->
    <uses-permission android:name="android.permission.FLASHLIGHT" /> <!-- 访问闪光灯 -->
    <uses-permission android:name="android.permission.VIBRATE" /> <!-- 允许访问振动设备 -->

    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" /><!-- 旧版的蓝牙权限，只要静态注册即可 -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" /><!-- 旧版的蓝牙权限，只要静态注册即可 -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />  <!-- 蓝牙扫描权限（Android 12.0 新增的权限） -->

    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" /> <!-- 蓝牙连接权限（Android 12.0 新增的权限）-->

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:name="android.hardware.camera.any" /> <!-- 适配Android R包可见性 开始 -->
    <uses-feature android:name="android.hardware.usb.host" />

    <queries package="${applicationId}">
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" /><!-- Android 11 使用相机需要 -->
        </intent>
        <intent>
            <action android:name="android.media.action.ACTION_VIDEO_CAPTURE" /><!-- Android 11 使用相机需要 -->
        </intent>

        <!-- 声明地图应用的包名，用于包可见性 -->
        <!-- 百度地图 -->
        <package android:name="com.baidu.BaiduMap" />
        <!-- 高德地图 -->
        <package android:name="com.autonavi.minimap" />
        <!-- 腾讯地图 -->
        <package android:name="com.tencent.map" />
        <!-- 谷歌地图 -->
        <package android:name="com.google.android.apps.maps" />
    </queries>

    <application
        android:name=".MCloudApplication"
        android:allowBackup="true"
        android:allowNativeHeapPointerTagging="false"
        android:enableOnBackInvokedCallback="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.mCloudApp"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning,UnusedAttribute,LockedOrientationActivity">


        <meta-data
            android:name="android.notch_support"
            android:value="true" /> <!-- 适配华为（huawei）刘海屏 -->

        <meta-data
            android:name="notch.config"
            android:value="portrait|landscape" /> <!-- 适配小米（xiaomi）刘海屏 -->

        <service
            android:name="com.baidu.location.f"
            android:enabled="true"
            android:foregroundServiceType="location"
            android:process=":remote" /><!-- 定位需要的服务 使用2.0的定位需要加上这个 -->

        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="${mapApikey}" />

        <meta-data
            android:name="BUGLY_APPID"
            android:value="${buglyAppId}" />  <!-- 配置APP ID -->

        <meta-data
            android:name="BUGLY_ENABLE_DEBUG"
            android:value="false" /><!-- 配置Bugly调试模式（true或者false）测试阶段建议设置成true，发布时设置为false。 -->

        <meta-data
            android:name="ScopedStorage"
            android:value="true" /><!-- 告知 XXPermissions 当前项目已经适配了分区存储特性 -->

        <activity
            android:name=".ui.page.welcome.SplashActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.page.login.LoginActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan|stateAlwaysHidden" />

        <activity
            android:name=".ui.page.main.MainActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/LightStatusBarActivity" />

        <activity
            android:name=".ui.page.device.DeviceHomeActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/LightStatusBarActivity" />

        <activity
            android:name=".ui.page.device.QuickFunctionActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/LightStatusBarActivity" />

        <activity
            android:name=".ui.page.webview.WebviewActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/LightStatusBarActivity" />

        <activity
            android:name=".ui.page.base.activity.ErrorActivity"
            android:label="错误界面"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/LightStatusBarActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Jetpack Startup Provider：统一调度各个 Initializer -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">

            <!-- MMKV -->
            <meta-data
                android:name="com.shmedo.core.commonlib.mmkv.MMKVInitializer"
                android:value="androidx.startup" />

            <!-- 网络监听 -->
            <meta-data
                android:name="com.shmedo.mcloudapp.startup.NetworkInitializer"
                android:value="androidx.startup" />

            <!-- 应用前后台生命周期 -->
            <meta-data
                android:name="com.shmedo.mcloudapp.startup.AppLifecycleInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>

</manifest>