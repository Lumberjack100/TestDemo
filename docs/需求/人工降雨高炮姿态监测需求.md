# 人工降雨高炮姿态监测需求

## 需求描述

参考 `com.shmedo.mcloudapp.ui.page.device.common.CommonLocationInfoFragment` 实现 `com.shmedo.mcloudapp.ui.page.device.gnss_product.fragment.gt600.Gt600LocationInfoFragment` 页面。在 Gt600LocationInfoFragment 页面中展示 基准站状态、移动站状态、倾角计状态、时间、位置、方位角、仰角。

1. 展示`QueryDeviceSensor`接口返回的 **"iotSensorType": "6007" 类型的第一个传感器** 的最新数据
2. 每秒自动请求`DescribeSensorNewData`接口刷新传感器数据

### 页面布局设计
固定横屏布局
1. 左侧展示卫星地图图层
   - 打点`DescribeSensorNewData`接口返回的经纬度，因为每秒刷新一次，所以要控制打点更新频率，计算当前经纬度与上一次经纬度的距离差值，如果差值大于10米，则刷新打点，否则不打点。
   - 不用实现导航功能，只展示打点。

2. 右侧区域展示基准站状态、移动站状态、倾角计状态、时间、位置(北纬,东经)、方位角、仰角。
 2.1 基准站状态判断：
    - 当 mode 字段是 68 时，基准站状态为正常，否则为异常。正常用绿色字体展示，异常用红色字体展示。
 2.2 移动站状态判断：
    - 当 mode 字段是 65 或者 68时，移动站状态为正常，否则为异常。正常用绿色字体展示，异常用红色字体展示。
 2.3 倾角计状态判断：
    - 计算 x_ang、y_ang、z_ang 三个轴的绝对值相加，如果等于 0 度，则倾角计状态为正常，否则为异常。正常用绿色字体展示，异常用红色字体展示。
 2.4 时间展示：
      - 展示`DescribeSensorNewData`接口返回的 time 字段。格式：2026年1月12日 09:08:16.000
 2.5 位置展示：
    - 展示`DescribeSensorNewData`接口返回的 lat 和 lon 字段。 格式：北纬31.212495°,东经121.594751°

---

## 技术实现方案

### 文件结构和命名规范

需要创建/修改的文件：

```
app/src/main/
├── java/com/shmedo/mcloudapp/
│   ├── ui/page/device/gnss_product/fragment/gt600/
│   │   └── Gt600LocationInfoFragment.kt    # 新建：姿态监测页面Fragment
│   └── ui/viewmodel/state/
│       └── Gt600LocationInfoViewModel.kt   # 新建：页面状态ViewModel
├── res/
│   ├── layout/
│   │   └── fragment_gt600_location_info.xml # 新建：横屏布局文件
│   └── navigation/
│       └── gt600_graph.xml                  # 修改：添加导航配置
```

### 架构模式

遵循项目 MVVM 架构：
- **Fragment**: 继承 `BaseFragment`，负责 UI 展示和用户交互
- **ViewModel**: 继承 `ViewModel`，管理页面状态（使用 `NonNullObservableField`）
- **Repository**: 复用 `DeviceManageRepositoryImp`，调用 API 获取数据

---

## 详细实现指南

### 1. 创建 ViewModel: `Gt600LocationInfoViewModel.kt`

**文件路径**: `app/src/main/java/com/shmedo/mcloudapp/ui/viewmodel/state/Gt600LocationInfoViewModel.kt`

```kotlin
package com.shmedo.mcloudapp.ui.viewmodel.state

import androidx.lifecycle.ViewModel
import com.shmedo.mcloudapp.ui.page.base.viewmodel.NonNullObservableField

/**
 * GT600 姿态监测页面 ViewModel
 */
class Gt600LocationInfoViewModel : ViewModel() {
    // 基准站状态（true=正常/绿色，false=异常/红色）
    val isBaseStationNormal = NonNullObservableField(false)
    val baseStationStatusText = NonNullObservableField("异常")
    
    // 移动站状态
    val isRoverStationNormal = NonNullObservableField(false)
    val roverStationStatusText = NonNullObservableField("异常")
    
    // 倾角计状态
    val isInclinometerNormal = NonNullObservableField(false)
    val inclinometerStatusText = NonNullObservableField("异常")
    
    // 时间
    val displayTime = NonNullObservableField("--")
    
    // 位置
    val displayLocation = NonNullObservableField("--")
    
    // 方位角
    val heading = NonNullObservableField("--")
    
    // 仰角 (x_ang)
    val elevation = NonNullObservableField("--")
    
    // 经纬度原始值（用于地图打点）
    val latitude = NonNullObservableField(0.0)
    val longitude = NonNullObservableField(0.0)
}
```

### 2. 创建布局文件: `fragment_gt600_location_info.xml`

**文件路径**: `app/src/main/res/layout/fragment_gt600_location_info.xml`

**布局要点**：
- 根布局使用 `LinearLayout`，`android:orientation="horizontal"` 实现左右分栏
- 左侧：百度地图 `MapView`，权重 weight=1
- 右侧：`ScrollView` 包裹状态信息列表，固定宽度 `280dp`

```xml
<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />
        
        <variable
            name="stateVM"
            type="com.shmedo.mcloudapp.ui.viewmodel.state.Gt600LocationInfoViewModel" />

        <variable
            name="click"
            type="com.shmedo.mcloudapp.ui.page.device.gnss_product.fragment.gt600.Gt600LocationInfoFragment.ClickProxy" />
    </data>

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/main_bg_gray"
        android:orientation="horizontal">

        <!-- 左侧：卫星地图 -->
        <FrameLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1">

            <com.baidu.mapapi.map.MapView
                android:id="@+id/mapView"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

            <!-- 返回按钮 -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|start"
                android:layout_marginStart="@dimen/dimen_size_16"
                android:layout_marginTop="@dimen/dimen_size_16"
                android:onClick="@{()->click.onBackPressed()}"
                app:backgroundTint="@color/white"
                app:fabSize="mini"
                app:srcCompat="@drawable/ic_arrow_back_24"
                app:tint="@color/black" />

            <!-- 回到当前位置按钮 -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|start"
                android:layout_marginStart="@dimen/dimen_size_16"
                android:layout_marginBottom="@dimen/dimen_size_16"
                android:onClick="@{()->click.backToLocation()}"
                app:backgroundTint="@color/white"
                app:fabSize="mini"
                app:srcCompat="@drawable/ic_map_locate"
                app:tint="@color/black" />

        </FrameLayout>

        <!-- 右侧：状态信息面板 -->
        <ScrollView
            android:layout_width="280dp"
            android:layout_height="match_parent"
            android:background="@color/white"
            android:fillViewport="true">

            <androidx.appcompat.widget.LinearLayoutCompat
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="@dimen/dimen_size_16">

                <!-- 标题 -->
                <com.google.android.material.textview.MaterialTextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="@dimen/dimen_size_16"
                    android:text="姿态监测"
                    android:textColor="@color/black"
                    android:textSize="18sp"
                    android:textStyle="bold" />

                <!-- 基准站状态 -->
                <include
                    layout="@layout/item_gt600_status_row"
                    app:label="@{`基准站状态`}"
                    app:value="@{stateVM.baseStationStatusText}"
                    app:isNormal="@{stateVM.isBaseStationNormal}" />

                <!-- 移动站状态 -->
                <include
                    layout="@layout/item_gt600_status_row"
                    app:label="@{`移动站状态`}"
                    app:value="@{stateVM.roverStationStatusText}"
                    app:isNormal="@{stateVM.isRoverStationNormal}" />

                <!-- 倾角计状态 -->
                <include
                    layout="@layout/item_gt600_status_row"
                    app:label="@{`倾角计状态`}"
                    app:value="@{stateVM.inclinometerStatusText}"
                    app:isNormal="@{stateVM.isInclinometerNormal}" />

                <View
                    android:layout_width="match_parent"
                    android:layout_height="1dp"
                    android:layout_marginVertical="@dimen/dimen_size_12"
                    android:background="@color/divider_color" />

                <!-- 时间 -->
                <include
                    layout="@layout/item_gt600_info_row"
                    app:label="@{`时间`}"
                    app:value="@{stateVM.displayTime}" />

                <!-- 位置 -->
                <include
                    layout="@layout/item_gt600_info_row"
                    app:label="@{`位置`}"
                    app:value="@{stateVM.displayLocation}" />

                <!-- 方位角 -->
                <include
                    layout="@layout/item_gt600_info_row"
                    app:label="@{`方位角`}"
                    app:value="@{stateVM.heading}" />

                <!-- 仰角 -->
                <include
                    layout="@layout/item_gt600_info_row"
                    app:label="@{`仰角`}"
                    app:value="@{stateVM.elevation}" />

            </androidx.appcompat.widget.LinearLayoutCompat>
        </ScrollView>

    </androidx.appcompat.widget.LinearLayoutCompat>
</layout>
```

### 3. 创建状态行子布局: `item_gt600_status_row.xml`

**文件路径**: `app/src/main/res/layout/item_gt600_status_row.xml`

```xml
<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.graphics.Color" />
        
        <variable name="label" type="String" />
        <variable name="value" type="String" />
        <variable name="isNormal" type="Boolean" />
    </data>

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/dimen_size_8"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.google.android.material.textview.MaterialTextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{label}"
            android:textColor="@color/sub_title_text_color_black_60"
            android:textSize="14sp"
            tools:text="基准站状态" />

        <com.google.android.material.textview.MaterialTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{value}"
            android:textColor="@{isNormal ? @color/status_normal_green : @color/status_error_red}"
            android:textSize="14sp"
            android:textStyle="bold"
            tools:text="正常" />

    </androidx.appcompat.widget.LinearLayoutCompat>
</layout>
```

### 4. 创建信息行子布局: `item_gt600_info_row.xml`

**文件路径**: `app/src/main/res/layout/item_gt600_info_row.xml`

```xml
<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable name="label" type="String" />
        <variable name="value" type="String" />
    </data>

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/dimen_size_8"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.google.android.material.textview.MaterialTextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{label}"
            android:textColor="@color/sub_title_text_color_black_60"
            android:textSize="14sp"
            tools:text="时间" />

        <com.google.android.material.textview.MaterialTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{value}"
            android:textColor="@color/black"
            android:textSize="14sp"
            tools:text="2026年1月12日 09:08:16.000" />

    </androidx.appcompat.widget.LinearLayoutCompat>
</layout>
```

### 5. 在 `colors.xml` 中添加状态颜色

**文件路径**: `app/src/main/res/values/colors.xml`（如果不存在则添加）

```xml
<!-- 状态颜色 -->
<color name="status_normal_green">#4CAF50</color>
<color name="status_error_red">#FF4400</color>
```

### 6. 创建 Fragment: `Gt600LocationInfoFragment.kt`

**文件路径**: `app/src/main/java/com/shmedo/mcloudapp/ui/page/device/gnss_product/fragment/gt600/Gt600LocationInfoFragment.kt`

```kotlin
package com.shmedo.mcloudapp.ui.page.device.gnss_product.fragment.gt600

import android.content.pm.ActivityInfo
import android.os.Bundle
import android.view.View
import androidx.fragment.app.viewModels
import com.baidu.mapapi.map.BaiduMap
import com.baidu.mapapi.map.BitmapDescriptorFactory
import com.baidu.mapapi.map.LogoPosition
import com.baidu.mapapi.map.MapStatusUpdateFactory
import com.baidu.mapapi.map.Marker
import com.baidu.mapapi.map.MarkerOptions
import com.baidu.mapapi.model.LatLng
import com.baidu.mapapi.utils.DistanceUtil
import com.kunminx.architecture.ui.page.DataBindingConfig
import com.shmedo.core.data.repository.DeviceManageRepositoryImp
import com.shmedo.mcloudapp.BR
import com.shmedo.mcloudapp.R
import com.shmedo.mcloudapp.baseclickproxy.BaseClickProxy
import com.shmedo.mcloudapp.databinding.FragmentGt600LocationInfoBinding
import com.shmedo.mcloudapp.extensions.launchWithViewLifecycle
import com.shmedo.mcloudapp.extensions.nav
import com.shmedo.mcloudapp.extensions.registerOnBackPressedDispatcher
import com.shmedo.mcloudapp.extensions.toGcj02LatLng
import com.shmedo.mcloudapp.ui.page.base.fragment.BaseFragment
import com.shmedo.mcloudapp.ui.viewmodel.state.Gt600LocationInfoViewModel
import com.shmedo.mcloudapp.utils.map.CustomLatLng
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.Job
import kotlinx.coroutines.delay
import kotlinx.coroutines.isActive
import kotlinx.coroutines.withContext
import org.koin.android.ext.android.inject
import timber.log.Timber
import java.text.SimpleDateFormat
import java.util.Locale
import kotlin.math.abs

/**
 * GT600 姿态监测页面
 * 
 * 功能：
 * 1. 固定横屏显示
 * 2. 左侧卫星地图展示设备位置
 * 3. 右侧展示设备状态信息（基准站、移动站、倾角计状态等）
 * 4. 每秒刷新传感器数据
 */
class Gt600LocationInfoFragment : BaseFragment() {

    private lateinit var binding: FragmentGt600LocationInfoBinding
    private val mStates: Gt600LocationInfoViewModel by viewModels()
    private val deviceManageRepository: DeviceManageRepositoryImp by inject()

    // 地图相关
    private lateinit var baiduMap: BaiduMap
    private var curMarker: Marker? = null
    private var lastLatLng: LatLng? = null // 上一次打点的经纬度
    private val mZoomLevel = 18f

    // 定时任务
    private var dataRefreshJob: Job? = null

    // 传感器ID（从 QueryDeviceSensor 获取）
    private var sensorId: String? = null

    // 设备Token（从 arguments 获取）
    private var deviceToken: String = ""

    // 距离阈值（米）- 超过此距离才更新地图打点
    companion object {
        private const val DISTANCE_THRESHOLD_METERS = 10.0
        private const val SENSOR_TYPE_6007 = "6007"
        private const val MODE_DIFFERENTIAL = 68  // D - 差分模式
        private const val MODE_AUTONOMOUS = 65    // A - 自主模式
    }

    override fun getDataBindingConfig(): DataBindingConfig {
        return DataBindingConfig(
            R.layout.fragment_gt600_location_info,
            BR.stateVM,
            mStates
        ).addBindingParam(BR.click, ClickProxy())
    }

    override fun initView(savedInstanceState: Bundle?) {
        binding = getBinding() as FragmentGt600LocationInfoBinding

        // 注册返回键处理
        registerOnBackPressedDispatcher {
            restoreOrientationAndNavigateUp()
        }

        // 初始化地图
        setupMap()
    }

    override fun initData() {
        // 获取设备Token
        arguments?.let {
            deviceToken = it.getString("deviceToken", "")
        }
    }

    override fun onResume() {
        super.onResume()
        // 设置固定横屏
        activity?.requestedOrientation = ActivityInfo.SCREEN_ORIENTATION_LANDSCAPE
        binding.mapView.onResume()
    }

    override fun onPause() {
        super.onPause()
        binding.mapView.onPause()
    }

    override fun lazyLoadData() {
        // 开始加载数据
        launchWithViewLifecycle {
            // 1. 先查询传感器ID
            querySensorId()
            
            // 2. 如果获取到传感器ID，开始定时刷新数据
            if (sensorId != null) {
                startDataRefreshTimer()
            }
        }
    }

    /**
     * 设置地图
     */
    private fun setupMap() {
        baiduMap = binding.mapView.map
        // 设置卫星地图图层
        baiduMap.mapType = BaiduMap.MAP_TYPE_SATELLITE
        baiduMap.setMaxAndMinZoomLevel(20f, 3f)
        baiduMap.setCompassEnable(false)
        binding.mapView.showZoomControls(false)
        binding.mapView.showScaleControl(false)
        binding.mapView.logoPosition = LogoPosition.logoPostionleftBottom
        baiduMap.setViewPadding(0, 0, 0, 0)

        // 默认位置（上海）
        moveCameraToLocation(LatLng(31.21032874, 121.59840681))
    }

    /**
     * 查询传感器ID
     */
    private suspend fun querySensorId() {
        try {
            val sensorList = withContext(Dispatchers.IO) {
                deviceManageRepository.queryDeviceSensorListWithPage(
                    deviceToken = deviceToken,
                    iotSensorType = SENSOR_TYPE_6007,
                    currentPage = 1,
                    pageSize = 1
                )?.currentPageData
            }

            // 获取第一个传感器的ID
            sensorId = sensorList?.firstOrNull()?.id?.toString()
            
            if (sensorId == null) {
                Timber.w("未找到类型为 $SENSOR_TYPE_6007 的传感器")
            } else {
                Timber.d("获取到传感器ID: $sensorId")
            }
        } catch (e: Exception) {
            Timber.e(e, "查询传感器ID失败")
        }
    }

    /**
     * 启动数据刷新定时器（每秒刷新）
     */
    private fun startDataRefreshTimer() {
        dataRefreshJob?.cancel()
        dataRefreshJob = launchWithViewLifecycle {
            while (isActive) {
                querySensorData()
                delay(1000) // 每秒刷新
            }
        }
    }

    /**
     * 查询传感器最新数据
     */
    private suspend fun querySensorData() {
        val currentSensorId = sensorId ?: return

        try {
            val dataList = withContext(Dispatchers.IO) {
                deviceManageRepository.querySensorNewData(currentSensorId)
            }

            // 获取第一条数据
            val data = dataList?.firstOrNull() ?: return

            // 更新UI
            updateUI(data)

        } catch (e: Exception) {
            Timber.e(e, "查询传感器数据失败")
        }
    }

    /**
     * 更新UI
     */
    private fun updateUI(data: Map<String, String>) {
        // 解析数据
        val mode = data["mode"]?.toIntOrNull() ?: 0
        val xAng = data["x_ang"]?.toDoubleOrNull() ?: 0.0
        val yAng = data["y_ang"]?.toDoubleOrNull() ?: 0.0
        val zAng = data["z_ang"]?.toDoubleOrNull() ?: 0.0
        val heading = data["heading"]?.toDoubleOrNull() ?: 0.0
        val lat = data["lat"]?.toDoubleOrNull() ?: 0.0
        val lon = data["lon"]?.toDoubleOrNull() ?: 0.0
        val time = data["time"] ?: ""

        // 1. 基准站状态：mode == 68 (D) 为正常
        val isBaseNormal = mode == MODE_DIFFERENTIAL
        mStates.isBaseStationNormal.set(isBaseNormal)
        mStates.baseStationStatusText.set(if (isBaseNormal) "正常" else "异常")

        // 2. 移动站状态：mode == 65 (A) 或 68 (D) 为正常
        val isRoverNormal = mode == MODE_AUTONOMOUS || mode == MODE_DIFFERENTIAL
        mStates.isRoverStationNormal.set(isRoverNormal)
        mStates.roverStationStatusText.set(if (isRoverNormal) "正常" else "异常")

        // 3. 倾角计状态：x_ang + y_ang + z_ang 绝对值之和 == 0 为正常
        val angSum = abs(xAng) + abs(yAng) + abs(zAng)
        val isInclinometerNormal = angSum == 0.0
        mStates.isInclinometerNormal.set(isInclinometerNormal)
        mStates.inclinometerStatusText.set(if (isInclinometerNormal) "正常" else "异常")

        // 4. 时间：格式化为 "2026年1月12日 09:08:16.000"
        mStates.displayTime.set(formatTime(time))

        // 5. 位置：格式化为 "北纬31.212495°,东经121.594751°"
        mStates.displayLocation.set(formatLocation(lat, lon))

        // 6. 方位角
        mStates.heading.set("${heading}°")

        // 7. 仰角 (x_ang)
        mStates.elevation.set("${xAng}°")

        // 8. 更新经纬度
        mStates.latitude.set(lat)
        mStates.longitude.set(lon)

        // 9. 更新地图打点（控制更新频率）
        updateMapMarker(lat, lon)
    }

    /**
     * 格式化时间
     * 输入: "2026-01-05 09:08:16.000"
     * 输出: "2026年1月5日 09:08:16.000"
     */
    private fun formatTime(timeStr: String): String {
        if (timeStr.isEmpty()) return "--"
        
        return try {
            val inputFormat = SimpleDateFormat("yyyy-MM-dd HH:mm:ss.SSS", Locale.getDefault())
            val outputFormat = SimpleDateFormat("yyyy年M月d日 HH:mm:ss.SSS", Locale.getDefault())
            val date = inputFormat.parse(timeStr)
            date?.let { outputFormat.format(it) } ?: timeStr
        } catch (e: Exception) {
            // 如果解析失败，尝试不带毫秒的格式
            try {
                val inputFormat = SimpleDateFormat("yyyy-MM-dd HH:mm:ss", Locale.getDefault())
                val outputFormat = SimpleDateFormat("yyyy年M月d日 HH:mm:ss", Locale.getDefault())
                val date = inputFormat.parse(timeStr)
                date?.let { outputFormat.format(it) } ?: timeStr
            } catch (e2: Exception) {
                timeStr
            }
        }
    }

    /**
     * 格式化位置
     * 输出: "北纬31.212495°,东经121.594751°"
     */
    private fun formatLocation(lat: Double, lon: Double): String {
        if (lat == 0.0 && lon == 0.0) return "--"
        return "北纬${lat}°,东经${lon}°"
    }

    /**
     * 更新地图打点
     * 只有当距离超过阈值时才更新
     */
    private fun updateMapMarker(lat: Double, lon: Double) {
        if (lat == 0.0 && lon == 0.0) return

        // WGS84 转 GCJ02（中国火星坐标系）
        val gcjLatLng = CustomLatLng(lat, lon).toGcj02LatLng()

        // 计算与上一次打点的距离
        val lastPoint = lastLatLng
        if (lastPoint != null) {
            val distance = DistanceUtil.getDistance(lastPoint, gcjLatLng)
            if (distance < DISTANCE_THRESHOLD_METERS) {
                // 距离小于阈值，不更新打点
                return
            }
        }

        // 更新打点
        addMarker(gcjLatLng)
        lastLatLng = gcjLatLng
    }

    /**
     * 添加地图标记
     */
    private fun addMarker(latLng: LatLng) {
        // 移除旧标记
        curMarker?.remove()
        curMarker = null

        try {
            val markerOption = MarkerOptions()
                .icon(BitmapDescriptorFactory.fromResource(R.drawable.ic_device_location))
                .position(latLng)
                .draggable(false)

            curMarker = baiduMap.addOverlay(markerOption) as Marker
            moveCameraToLocation(latLng)
        } catch (ex: Exception) {
            Timber.e(ex, "添加地图标记失败")
        }
    }

    /**
     * 移动地图到指定位置
     */
    private fun moveCameraToLocation(latLng: LatLng) {
        baiduMap.setMapStatus(MapStatusUpdateFactory.newLatLngZoom(latLng, mZoomLevel))
    }

    /**
     * 恢复屏幕方向并返回
     */
    private fun restoreOrientationAndNavigateUp() {
        activity?.requestedOrientation = ActivityInfo.SCREEN_ORIENTATION_PORTRAIT
        nav().navigateUp()
    }

    /**
     * 点击事件代理
     */
    inner class ClickProxy : BaseClickProxy() {
        /**
         * 返回按钮点击
         */
        fun onBackPressed() {
            restoreOrientationAndNavigateUp()
        }

        /**
         * 回到当前位置
         */
        fun backToLocation() {
            lastLatLng?.let {
                moveCameraToLocation(it)
            }
        }
    }

    override fun onSaveInstanceState(outState: Bundle) {
        super.onSaveInstanceState(outState)
        binding.mapView.onSaveInstanceState(outState)
    }

    override fun onDestroy() {
        dataRefreshJob?.cancel()
        // 恢复屏幕方向
        activity?.requestedOrientation = ActivityInfo.SCREEN_ORIENTATION_PORTRAIT
        super.onDestroy()
        binding.mapView.onDestroy()
    }

    companion object {
        /**
         * 创建 Bundle 参数
         */
        fun newBundleArguments(deviceToken: String): Bundle {
            return Bundle().apply {
                putString("deviceToken", deviceToken)
            }
        }
    }
}
```

### 7. 修改导航配置: `gt600_graph.xml`

**文件路径**: `app/src/main/res/navigation/gt600_graph.xml`

在现有配置基础上添加：

```xml
<!-- 在 </navigation> 之前添加 -->

<!-- GT600 姿态监测页面 -->
<fragment
    android:id="@+id/gt600LocationInfoFragment"
    android:name="com.shmedo.mcloudapp.ui.page.device.gnss_product.fragment.gt600.Gt600LocationInfoFragment"
    android:label="Gt600LocationInfoFragment"
    tools:layout="@layout/fragment_gt600_location_info" />

<action
    android:id="@+id/action_global_to_gt600LocationInfoFragment"
    app:destination="@id/gt600LocationInfoFragment"
    app:enterAnim="@anim/activity_open_enter"
    app:exitAnim="@anim/activity_open_exit"
    app:popEnterAnim="@anim/activity_close_enter"
    app:popExitAnim="@anim/activity_close_exit" />
```

### 8. 修改 GT600HomeFragment 模块配置

**文件路径**: `app/src/main/java/com/shmedo/mcloudapp/ui/page/device/gnss_product/fragment/gt600/GT600HomeFragment.kt`

将"位置信息"模块的导航目标改为新页面：

```kotlin
// 找到原来的位置信息模块配置，修改 navId
CommonModule(
    name = "姿态监测",  // 修改名称
    resID = R.drawable.ic_module_location_info,
    iconSize = ConvertUtils.dp2px(34f),
    navId = R.id.action_global_to_gt600LocationInfoFragment,  // 修改导航ID
    isSupport = true
).toUnified()
```

同时需要重写 `processOtherItemClick` 方法传递参数：

```kotlin
override fun processOtherItemClick(configModule: DeviceFunctionModule) {
    when (configModule) {
        is DataCenterModule -> {
            // 保持原有逻辑
            nav().safeNavigate(
                configModule.navId,
                BaseDataCenterHomeFragment.newBundleArguments(
                    centerNum = 4,
                    productType,
                    communicateWay,
                    deviceInfo,
                    bleDevice
                )
            )
        }
        
        is CommonModule -> {
            if (configModule.navId == R.id.action_global_to_gt600LocationInfoFragment) {
                // 姿态监测页面需要传递 deviceToken
                nav().safeNavigate(
                    configModule.navId,
                    Gt600LocationInfoFragment.newBundleArguments(deviceInfo.deviceToken)
                )
            } else {
                super.processOtherItemClick(configModule)
            }
        }

        else -> {
            super.processOtherItemClick(configModule)
        }
    }
}
```

---

## API接口参考

### QueryDeviceSensor
**请求参数**：
```json
{
	"deviceToken": "2400006GT1",
	"iotSensorType": "6007",
	"sensorValid": true,
	"pageSize": 20,
	"currentPage": 1
}
```

**响应示例**：
```json
{
    "totalCount": 1,
    "totalPage": 1,
    "currentPageData": [
        {
            "id": 225764,
            "name": "6007_1",
            "alias": "6007_1",
            "dataProtocol": "6007",
            "uniqueToken": "9AFD42AE44154939A9EBA6791C923E73",
            "installLocation": null,
            "gpsLocation": null,
            "status": 0,
            "iotSensorType": "6007",
            "sensorValid": true,
            "warnNoData": true,
            "exValues": null,
            "note": null,
            "monitorBeginTime": "2026-01-04 17:08:22",
            "deviceID": null,
            "deviceToken": null,
            "productID": null,
            "productToken": null,
            "productName": null,
            "iotSensorTypeStr": "GNSS组合导航姿态",
            "createDeviceToken": "2400006GT1",
            "fusionSensor": false
        }
    ]
}
```

### DescribeSensorNewData
**请求参数**：
```json
{
  "sensorID": 225764
}
```

**响应示例**：
```json
[
    {
        "mode": 65,
        "y_ang": -0.322,
        "x_ang": -15.32,
        "heading": 229.9,
        "geneID": "GENE_5DBD7A8699014D30B70E5CFA44A02D01@20175800418",
        "z_ang": -74.722,
        "alt": 35.4,
        "lon": 121.594751,
        "time": "2026-01-05 09:08:16.000",
        "speed": 0,
        "lat": 31.212495,
        "sensorID": 225764
    }
]
```

**字段说明**：
|字段|说明|备注|
|---|---|---|
|mode|模式（A：自主模式，D：差分模式 E：惯导模式 M：手动输入模式 N：数据不可用 P：高精度模式 S：模拟器模式 ）|DescribeSensorNewData 接口返回的 mode 字段是 ASCII 码对应的 10 进制，比如 65 对应 A，68 对应 D|
|x_ang|x 轴角度|对应仰角|
|y_ang|y 轴角度||
|z_ang|z 轴角度||
|heading|方位角|对应方位角|
|alt|海拔高度||
|lon|经度||
|lat|纬度||
|time|时间||
|speed|速度||
|sensorID|传感器ID||

---

## 实现检查清单

实现完成后，请检查以下项目：

- [ ] `Gt600LocationInfoViewModel.kt` 已创建
- [ ] `fragment_gt600_location_info.xml` 已创建
- [ ] `item_gt600_status_row.xml` 已创建
- [ ] `item_gt600_info_row.xml` 已创建
- [ ] `colors.xml` 中已添加状态颜色
- [ ] `Gt600LocationInfoFragment.kt` 已创建
- [ ] `gt600_graph.xml` 已添加导航配置
- [ ] `GT600HomeFragment.kt` 已修改模块配置
- [ ] 页面进入时自动切换为横屏
- [ ] 页面退出时恢复为竖屏
- [ ] 地图显示为卫星图层
- [ ] 每秒刷新传感器数据
- [ ] 地图打点距离控制正常（>10米才更新）
- [ ] 基准站状态判断正确（mode=68为正常）
- [ ] 移动站状态判断正确（mode=65或68为正常）
- [ ] 倾角计状态判断正确（三轴角度绝对值之和=0为正常）
- [ ] 时间格式正确（yyyy年M月d日 HH:mm:ss.SSS）
- [ ] 位置格式正确（北纬xx°,东经xx°）
- [ ] 状态正常显示绿色，异常显示红色

---

## 测试验收标准

1. **页面显示**
   - 进入页面后自动切换为横屏模式
   - 左侧显示卫星地图，右侧显示状态信息面板
   - 返回时恢复竖屏模式

2. **数据刷新**
   - 页面加载后自动开始每秒刷新数据
   - 状态信息实时更新

3. **地图功能**
   - 地图显示卫星图层
   - 设备位置正确打点
   - 位置变化超过10米时更新打点
   - 点击定位按钮可回到当前设备位置

4. **状态判断**
   - 基准站状态：mode=68时显示绿色"正常"，否则显示红色"异常"
   - 移动站状态：mode=65或68时显示绿色"正常"，否则显示红色"异常"
   - 倾角计状态：三轴角度绝对值之和为0时显示绿色"正常"，否则显示红色"异常"

5. **数据格式**
   - 时间格式：2026年1月5日 09:08:16.000
   - 位置格式：北纬31.212495°,东经121.594751°
   - 方位角/仰角格式：xxx.x°

---

# 人工降雨高炮姿态监测需求迭代

## 需求描述

在姿态监测页面右侧数据展示区域下方添加一个"保存"按钮，点击保存按钮后，将当前页面截屏保存到相册。

---

# 人工降雨高炮姿态监测需求迭代

## 需求描述

我已经将页面中卫星地图图层设置为“永远保持正北朝上”，关闭旋转/俯视手势。我在 drawable 文件下添加了一个表示高射炮车的图标 `R.drawable.anti_aircraft_gun`，请在卫星地图图层中展示该图标，此时车头朝向正北。需要根据 DescribeSensorNewData 接口实时返回的 heading(方位角) 字段，计算出炮车车头朝向，并设置图标从正北方向顺时针旋转角度。

---

# 人工降雨高炮姿态监测需求迭代

## 需求描述

 DescribeSensorNewData 接口实时返回的 heading(方位角) 字段，与上一次 heading(方位角) 字段 绝对值的差值大于 5 度，则更新地图图层中的图标旋转角度。

 ---


# 人工降雨高炮姿态监测需求迭代

## 需求描述

当我将手机系统导航栏方式设置为显示导航键后，我发现页面右侧区域数据展示区域被导航栏遮挡了，如何进行优化，避免被导航栏遮挡。